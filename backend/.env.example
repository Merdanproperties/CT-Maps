# Database URL - when running imports from host, use Docker Postgres (host port 5433):
#   docker compose up -d postgres   # Docker Postgres listens on host 5433
#   Then run imports; they write to Docker. No sync needed.
# DATABASE_URL=postgresql+psycopg2://ctmaps:ctmaps@127.0.0.1:5433/ct_properties
# (Backend container uses postgres:5432 internally; 5433 avoids conflict with local Postgres on 5432.)

# Optional: local Nominatim for geocoding scripts (e.g. mediagis/nominatim on port 8080).
# Not required for the web app.
# NOMINATIM_URL=http://localhost:8080
