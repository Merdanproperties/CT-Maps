# Database URL - when running locally use localhost; in Docker Compose this is set by compose.
# Explicit driver (psycopg2) avoids ambiguity: postgresql+psycopg2://user:password@host:5432/dbname
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/ct_properties

# Optional: local Nominatim for geocoding scripts (e.g. mediagis/nominatim on port 8080).
# Not required for the web app.
# NOMINATIM_URL=http://localhost:8080
