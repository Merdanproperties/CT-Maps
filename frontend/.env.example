# Mapbox Access Token (required for map; get from Mapbox console)
# VITE_MAPBOX_ACCESS_TOKEN=pk.your_token_here

# Proxy target: leave unset for local dev (defaults to http://localhost:8000).
# Docker Compose sets VITE_PROXY_TARGET=http://backend:8000 automatically.
# VITE_PROXY_TARGET=http://localhost:8000

# Map Provider: 'auto', 'mapbox', 'leaflet'
# VITE_MAP_PROVIDER=auto

# Mapbox style URL (optional)
# VITE_MAPBOX_STYLE=mapbox://styles/mapbox/streets-v12
